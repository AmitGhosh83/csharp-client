<Window x:Class="WpfApp2_PracticeBInding.ResourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2_PracticeBInding"
        mc:Ignorable="d"
        Title="{StaticResource ApplicationName}" Height="450" Width="800">
    <Window.Resources>

    </Window.Resources>

    <StackPanel>
        <TextBox x:Name="uxName" Style="{StaticResource ResourceKey=Input}" />
        <TextBox x:Name="uxPassword" Style="{DynamicResource ResourceKey=Password}" />
    </StackPanel>
</Window>
